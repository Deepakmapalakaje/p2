# Flask Secret Key (generate a strong random one using Python: python -c 'import secrets; print(secrets.token_hex(32))')
FLASK_SECRET_KEY=e84764c8447077ce1b3742f56006f71aee1eeebe494d24c2fa82b637a9dad2ad

# Set to 1 to enable the trading pipeline during market hours
RUN_PIPELINE=1

# Port for the Flask app to run on (default 8080)
PORT=8080

# Paths to databases (using dedicated 'database/' folder as recommended)
TRADING_DB=database/upstox_v3_live_trading.db
USER_DB=database/users.db

# Optional: Email settings (if not hardcoded in email_utils.py)
SENDER_EMAIL="dscatreeing@gmail.com"
SENDER_PASSWORD="lszl urfy lhlm vshz"  # App-specific password for Gmail
